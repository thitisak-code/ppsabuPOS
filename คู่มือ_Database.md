# คู่มือการใช้งาน - ระบบ POS เพลิดเพลินชาบู (Database Version)

## 🎯 ข้อดีของเวอร์ชัน Database

### เปรียบเทียบ JSON vs SQLite Database

| คุณสมบัติ | JSON (เก่า) | SQLite Database (ใหม่) |
|-----------|-------------|------------------------|
| ความเร็วในการค้นหา | ช้า | **เร็วมาก** ⚡ |
| ขนาดข้อมูลสูงสุด | จำกัด (~1000 รายการ) | **ไม่จำกัด** 📊 |
| ความปลอดภัย | ปานกลาง | **สูง** 🔒 |
| การจัดการข้อมูล | ธรรมดา | **มีโครงสร้าง** 📁 |
| ความเสถียร | ธรรมดา | **สูงมาก** 💪 |
| สามารถกู้คืนข้อมูล | ยาก | **ง่าย** ✅ |

## 📦 ความต้องการของระบบ
- ระบบปฏิบัติการ: Windows 7/8/10/11
- หน่วยความจำ (RAM): 2 GB ขึ้นไป
- พื้นที่ว่าง: 100 MB

## 🚀 วิธีแปลงเป็นไฟล์ .exe

### ขั้นตอนที่ 1: ติดตั้ง Python (ถ้ายังไม่มี)
1. ดาวน์โหลด Python จาก https://www.python.org/downloads/
2. ติดตั้งและเลือก "Add Python to PATH"

### ขั้นตอนที่ 2: สร้างไฟล์ .exe
1. เปิด Command Prompt หรือ Terminal
2. ไปที่โฟลเดอร์ที่มีไฟล์
   ```
   cd path/to/folder
   ```
3. รันคำสั่ง:
   ```
   python build_exe_db.py
   ```
4. รอจนเสร็จ (ประมาณ 1-2 นาที)
5. ไฟล์ .exe จะอยู่ในโฟลเดอร์ `dist`

### ขั้นตอนที่ 3: ใช้งานไฟล์ .exe
1. คัดลอกไฟล์ `.exe` จากโฟลเดอร์ `dist` ไปไว้ที่ต่างหาก
2. ดับเบิลคลิกเพื่อเปิดโปรแกรม
3. ไม่ต้องติดตั้ง Python แล้ว!

## 💡 คุณสมบัติเด่น

### 1. **ไม่กินแรม**
- เปิดเมื่อต้องการใช้งาน
- ปิดเมื่อไม่ใช้
- ข้อมูลถูกบันทึกในฐานข้อมูล SQLite

### 2. **ระบบฐานข้อมูล SQLite**
- **รวดเร็ว:** ค้นหาข้อมูลได้ทันที
- **ปลอดภัย:** ข้อมูลไม่เสียหายง่าย
- **ทนทาน:** รองรับข้อมูลนับหมื่นรายการ
- **มาตรฐาน:** ใช้เทคโนโลยีเดียวกับ Chrome, Firefox

### 3. **ค้นหาประวัติการขายแบบเรียลไทม์**
- ค้นหาจาก Bill ID, โต๊ะ, วันที่, ยอดรวม
- แสดงผลลัพธ์ทันที
- รองรับข้อมูลจำนวนมาก

### 4. **บันทึกอัตโนมัติ**
- บันทึกทันทีเมื่อมีการเปลี่ยนแปลง
- ไม่ต้องกดบันทึก
- ปิดโปรแกรมได้เลย ข้อมูลจะไม่หาย

## 📖 วิธีใช้งาน

### การจัดการโต๊ะ
- **สีเขียว** = โต๊ะที่กำลังเลือกอยู่
- **สีแดง** = โต๊ะที่มีลูกค้า
- **สีเทา** = โต๊ะว่าง

### การเพิ่มรายการอาหาร
1. เลือกโต๊ะ
2. คลิกเมนูอาหารที่ต้องการ
3. รายการจะแสดงทางขวามือและบันทึกอัตโนมัติ

### การชำระเงิน
1. ตรวจสอบรายการ
2. คลิก "💰 ชำระเงิน"
3. ใบเสร็จจะแสดงขึ้น
4. บิลจะถูกบันทึกในประวัติอัตโนมัติ
5. โต๊ะจะว่างอัตโนมัติ

### การค้นหาประวัติ
1. คลิก "📜 ประวัติการขาย"
2. พิมพ์คำค้นหา (ค้นหาได้ทันที)
3. เลือกประเภทการค้นหา
4. ผลลัพธ์แสดงแบบเรียลไทม์

## 📁 ไฟล์ข้อมูล

### shabu_pos.db
ไฟล์ฐานข้อมูล SQLite ที่เก็บ:
- รายการเมนูอาหาร (ตาราง menu_items)
- ข้อมูลโต๊ะ (ตาราง tables)
- ออเดอร์ปัจจุบัน (ตาราง orders)
- ประวัติการขาย (ตาราง sales_history)
- รายการในบิล (ตาราง sale_items)

**สำคัญ:** อย่าลบไฟล์นี้ ข้อมูลทั้งหมดจะหาย!

### โครงสร้างฐานข้อมูล

```
📊 shabu_pos.db
├── 📋 menu_items (เมนูอาหาร)
│   ├── id (รหัสอัตโนมัติ)
│   ├── name (ชื่อเมนู)
│   ├── price (ราคา)
│   ├── created_at (วันที่สร้าง)
│   └── updated_at (วันที่แก้ไข)
│
├── 📋 tables (โต๊ะ)
│   ├── id (รหัสอัตโนมัติ)
│   ├── table_name (ชื่อโต๊ะ)
│   └── created_at (วันที่สร้าง)
│
├── 📋 orders (ออเดอร์ปัจจุบัน)
│   ├── id (รหัสอัตโนมัติ)
│   ├── table_id (โต๊ะ)
│   ├── menu_item_id (รหัสเมนู)
│   ├── menu_name (ชื่อเมนู)
│   ├── price (ราคา)
│   └── created_at (วันที่สั่ง)
│
├── 📋 sales_history (ประวัติการขาย)
│   ├── id (รหัสอัตโนมัติ)
│   ├── bill_id (รหัสบิล)
│   ├── table_name (ชื่อโต๊ะ)
│   ├── total_amount (ยอดรวม)
│   └── created_at (วันที่ชำระ)
│
└── 📋 sale_items (รายการในบิล)
    ├── id (รหัสอัตโนมัติ)
    ├── sale_id (รหัสบิล)
    ├── menu_name (ชื่อเมนู)
    └── price (ราคา)
```

## 🔧 การสำรองข้อมูล

### วิธีที่ 1: คัดลอกไฟล์ Database
```
คัดลอก shabu_pos.db ไปไว้ที่อื่น
```

### วิธีที่ 2: ใช้เครื่องมือ SQLite
```
1. ดาวน์โหลด DB Browser for SQLite
2. เปิดไฟล์ shabu_pos.db
3. Export เป็น SQL Script
```

### วิธีที่ 3: Backup อัตโนมัติ (แนะนำ)
```
สำรองทุกวัน:
- คัดลอก shabu_pos.db
- เปลี่ยนชื่อเป็น shabu_pos_backup_YYYYMMDD.db
```

## 🛠️ เครื่องมือสำหรับจัดการฐานข้อมูล

### DB Browser for SQLite (แนะนำ)
- ดาวน์โหลด: https://sqlitebrowser.org/
- ฟรี และใช้งานง่าย
- ดูข้อมูล, แก้ไข, Export ได้

### ฟีเจอร์ที่ทำได้:
- ✅ ดูข้อมูลทั้งหมด
- ✅ ค้นหาแบบละเอียด
- ✅ Export เป็น CSV, Excel
- ✅ สร้างรายงาน
- ✅ กู้คืนข้อมูล

## ❓ แก้ปัญหา

### โปรแกรมไม่เปิด
1. ตรวจสอบไฟล์ .exe ไม่เสียหาย
2. ลอง Run as Administrator
3. ตรวจสอบว่ามีไฟล์ database.py อยู่ในโฟลเดอร์เดียวกัน

### ข้อมูลหาย
1. ตรวจสอบไฟล์ shabu_pos.db ยังอยู่หรือไม่
2. กู้คืนจาก Backup
3. ใช้ DB Browser for SQLite เปิดดู

### โปรแกรมช้า
1. ปิดโปรแกรมอื่นที่ไม่ใช้
2. ตรวจสอบขนาดฐานข้อมูล (ถ้าใหญ่เกิน 100 MB ให้ล้างประวัติเก่า)
3. ใช้ DB Browser ทำ Vacuum Database

### Database Error
1. ปิดโปรแกรมทั้งหมดที่เปิดไฟล์ .db
2. ตรวจสอบว่าไม่มีไฟล์ .db-journal หรือ .db-wal
3. กู้คืนจาก Backup

## 📊 การวิเคราะห์ข้อมูล

### ดูยอดขายรวม
```sql
SELECT SUM(total_amount) as total_sales 
FROM sales_history;
```

### ดูเมนูขายดี
```sql
SELECT menu_name, COUNT(*) as count, SUM(price) as total
FROM sale_items
GROUP BY menu_name
ORDER BY count DESC
LIMIT 10;
```

### ดูยอดขายแต่ละวัน
```sql
SELECT DATE(created_at) as date, SUM(total_amount) as daily_total
FROM sales_history
GROUP BY DATE(created_at)
ORDER BY date DESC;
```

## 🎓 เคล็ดลับการใช้งาน

### 1. สำรองข้อมูลเป็นประจำ
- ทุกวัน: คัดลอกไฟล์ .db
- ทุกสัปดาห์: Export เป็น SQL Script

### 2. ตรวจสอบฐานข้อมูล
- เปิด DB Browser เป็นครั้งคราว
- ตรวจสอบความผิดปกติ

### 3. ล้างข้อมูลเก่า
- ลบประวัติการขายที่เก่ากว่า 1 ปี
- Export ไว้ก่อนลบ

### 4. ใช้ค้นหาให้เป็นประโยชน์
- ค้นหายอดขายวันนี้
- ค้นหาบิลที่มีปัญหา
- วิเคราะห์พฤติกรรมลูกค้า

## 📞 ติดต่อ/สอบถาม
หากมีปัญหาหรือข้อสงสัย สามารถติดต่อได้ที่:
- Email: support@example.com
- Tel: 02-XXX-XXXX

## 📋 เวอร์ชัน
- v1.0 - เวอร์ชันแรก (ใช้ JSON)
- v1.1 - เพิ่มฟังก์ชันค้นหา
- v1.2 - แปลงเป็น .exe ได้
- **v2.0 - ใช้ SQLite Database** 🎉

---
พัฒนาโดย: ทีมพัฒนา POS System
อัปเดตล่าสุด: 2024
เวอร์ชัน: 2.0 (Database Edition)
